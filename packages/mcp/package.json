{
  "name": "@nock/mcp",
  "version": "0.1.0",
  "description": "Nock MCP server",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist || true",
    "test": "vitest run"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.0",
    "@nock/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}

