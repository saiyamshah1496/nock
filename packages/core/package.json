{
  "name": "@nock/core",
  "version": "0.1.0",
  "description": "Nock core: parse → classify → rules → VerdictV1",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist || true",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "yaml": "^2.5.0"
  }
}
