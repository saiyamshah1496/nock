{
  "name": "@nock/action",
  "version": "0.1.0",
  "private": true,
  "description": "Nock GitHub Action wrapper",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist || true",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "@nock/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}

