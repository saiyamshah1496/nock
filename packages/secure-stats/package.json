{
  "name": "@nock/secure-stats",
  "version": "0.1.0",
  "description": "Nock secure stats envelope (AES-GCM) utils",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rimraf dist || true",
    "test": "vitest run"
  },
  "dependencies": {
    "@nock/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}

