# Nock waitlist (marketing one-pager)

Static HTML/CSS/JS for Cloudflare Pages (`nock-waitlist`). Product-owned design — not the Inter/teal draft.

## Preview
```bash
cd waitlist && python3 -m http.server 8080
```

## Form
`config.js` → Formspree `https://formspree.io/f/xyezgqoy` (same as PR #3).

## Deploy
Pages root should be `waitlist/` — coordinate with Founding Engineer.

## Typography
- Display: Instrument Serif (hero / section titles)
- UI/body: Geist
- Code: Geist Mono
